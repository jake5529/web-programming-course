# Лабораторная работа 6: API Fundamentals - Работа с реальным API и essay вопросы

## Описание

Эта лабораторная работа является продолжением LR5. В предыдущей работе вы подключили mock API и реализовали вопросы с множественным выбором (multiple-select). Теперь вы:

1. **Переключитесь на реальный внешний API сервер** вместо локального mock-сервера
2. **Добавите поддержку нового типа вопросов** - essay (текстовые развернутые ответы)

**Цель:** Научиться работать с внешним API и обрабатывать разные типы данных в зависимости от типа вопроса.

---

## Что вы реализовали в LR5

- ✅ Интеграция с mock API
- ✅ Создание игровой сессии
- ✅ Отображение вопросов типа **multiple-select** (множественный выбор)
- ✅ Отправка выбранных ответов на сервер
- ✅ Завершение сессии и получение результатов

## Что нужно реализовать в LR6

- 🎯 Переключиться на внешний API сервер
- 🎯 Добавить поддержку вопросов типа **essay** (текстовые ответы)
- 🎯 Реализовать UI для ввода текстового ответа
- 🎯 Обработать различия в отправке данных для разных типов вопросов

---

## Шаг 0: Подготовка окружения

### Вариант 1: Продолжение в той же директории lr5

```bash
cd lessons/lr5
```

### Вариант 2: Создание отдельной ветки lr6

```bash
cd lessons/lr5

# Сохраните текущую работу
git add .
git commit -m "Complete LR5"

# Создайте новую ветку
git checkout -b lr6
```

---

## Шаг 1: Получение изменений из upstream

Преподаватель внес изменения в кодовую базу. Необходимо их получить:

```bash
# Получите последние изменения
git fetch upstream

# Влейте изменения в вашу ветку
git merge upstream/main
```

**⚠️ Важно:** Могут возникнуть конфликты слияния. Разрешите их вручную:

1. Откройте файлы с конфликтами в редакторе
2. Найдите маркеры `<<<<<<<`, `=======`, `>>>>>>>`
3. Оставьте нужные изменения, удалите маркеры
4. Сохраните и закоммитьте:

```bash
git add .
git commit -m "Resolve merge conflicts"
```

---

## Шаг 2: Настройка переменных окружения

После слияния в проекте появится файл `.env.example`:

```bash
# Скопируйте его в .env
cp .env.example .env
```

### Содержимое `.env.example`:

```env
# GitHub OAuth
VITE_GITHUB_CLIENT_ID=Ov23liELvyQUKMmRdXlw

# Redirect URI
VITE_GITHUB_REDIRECT_URI=http://localhost:5173

# API Backend URL
VITE_API_URL=http://dancv.ddns.net
```

### 🔴 Важное изменение для LR6:

В `.env` файле **НЕ МЕНЯЙТЕ** значение `VITE_API_URL`. Теперь вы работаете с **реальным внешним сервером**, а не с локальным mock-сервером.

**Было в LR5:**
```env
VITE_API_URL=http://localhost:3000  # mock-сервер
```

**Стало в LR6:**
```env
VITE_API_URL=http://dancv.ddns.net  # реальный сервер преподавателя
```

---

## Шаг 3: Обновление зависимостей

После слияния могли появиться новые пакеты:

```bash
npm install
```

Это обновит:
- `@course/auth-component` - компонент авторизации
- Другие зависимости, если добавлены

---

## Шаг 4: Перегенерация API хуков

Настройки Orval или OpenAPI схема могли измениться:

```bash
npm run codegen
```

Это обновит директорию `generated/api/` с актуальными типами и хуками.

---

## Шаг 5: Запуск приложения

### 5.1 Очистите кеш Vite

```bash
rm -rf node_modules/.vite
```

### 5.2 Запустите dev-сервер

```bash
npm run dev -- --force
```

Приложение откроется на `http://localhost:5173`

### 5.3 Mock-сервер больше не нужен!

⚠️ **Важно:** Для LR6 вам **НЕ нужно** запускать локальный mock-сервер. Вы работаете с реальным внешним API.

---

## Шаг 6: Реализация поддержки essay вопросов

### 6.1 Изучите структуру типов вопросов

API теперь возвращает два типа вопросов:

**Multiple-select (уже реализовано в LR5):**
```typescript
{
  id: "q_123",
  type: "multiple-select",
  question: "Какие хуки используются для side effects?",
  options: ["useState", "useEffect", "useLayoutEffect"],
  maxPoints: 4,
  difficulty: "medium"
}
```

**Essay (нужно реализовать):**
```typescript
{
  id: "q_456",
  type: "essay",
  question: "Объясните разницу между useMemo и useCallback",
  minLength: 100,
  maxLength: 1000,
  maxPoints: 10,
  difficulty: "hard"
}
```

## Шаг 7: Проверка работы

### 7.1 Тестирование с реальным API

1. **Авторизация**: Нажмите "Login" → авторизуйтесь через GitHub
2. **Старт игры**: Нажмите "Начать игру"
3. **Вопросы multiple-select**:
   - Видны варианты ответов
   - Можно выбрать несколько
   - Работает отправка на сервер
4. **Вопросы essay**:
   - Видно поле для ввода текста
   - Отображается счетчик символов
   - Кнопка "Далее" активна только при достижении минимальной длины
5. **Завершение**: После последнего вопроса сессия завершается

### 7.2 Проверка в DevTools

Откройте DevTools (F12):

- **Console**: нет ошибок
- **Network**:
  - Запросы идут на `http://dancv.ddns.net/api/*`
  - Видны запросы `POST /api/sessions/{id}/answers` с разными данными:
    - `{"selectedOptions": [0, 2]}` для multiple-select
    - `{"textAnswer": "..."}` для essay
- **Application → Local Storage**: токен авторизации сохранен

---

## Отличия LR6 от LR5

| Аспект | LR5 | LR6 |
|--------|-----|-----|
| **API сервер** | Mock (localhost:3000) | Реальный (dancv.ddns.net) |
| **Типы вопросов** | Только multiple-select | multiple-select + essay |
| **Mock-сервер** | Нужен запуск | Не нужен |
| **Ответы** | Только массив индексов | Индексы ИЛИ текст |
| **UI компоненты** | Только кнопки выбора | Кнопки + textarea |

---

## Частые проблемы и решения

### Проблема: Не приходят вопросы с сервера

**Решение:**
1. Проверьте `.env` - должно быть `VITE_API_URL=http://dancv.ddns.net`
2. Перезапустите dev-сервер после изменения `.env`
3. Проверьте Network tab - запросы должны идти на внешний сервер

### Проблема: Ошибка "Cannot read property 'type' of undefined"

**Решение:**
Добавьте проверки в коде:

```typescript
if (!currentQuestion) return null;

// Дальше безопасно использовать currentQuestion.type
```

### Проблема: Не отправляется текстовый ответ для essay

**Решение:**
Проверьте формат отправки в Network tab. Должно быть:

```json
{
  "questionId": "q_456",
  "textAnswer": "Ваш текст ответа..."
}
```

А НЕ:

```json
{
  "questionId": "q_456",
  "selectedOptions": []  // ❌ Неправильно для essay
}
```

### Проблема: Конфликты при git merge

**Решение:**

```bash
# Посмотрите какие файлы конфликтуют
git status

# Откройте файлы и разрешите конфликты вручную
# Затем:
git add .
git commit -m "Resolve conflicts"
```

### Проблема: Стили не применяются

**Решение:**

```bash
rm -rf node_modules/.vite
npm run dev -- --force
```

---

## Критерии оценки

- ✅ Приложение успешно работает с внешним API
- ✅ Корректно отображаются вопросы обоих типов (multiple-select и essay)
- ✅ Для multiple-select работает множественный выбор
- ✅ Для essay работает ввод текста с проверкой длины
- ✅ Ответы корректно отправляются на сервер в нужном формате
- ✅ Сессия успешно завершается
- ✅ Нет ошибок в консоли

---

## Полезные команды

```bash
# Проверить текущий URL API
cat .env | grep VITE_API_URL

# Посмотреть изменения из upstream
git fetch upstream
git diff upstream/main

# Полная перезагрузка окружения
rm -rf node_modules package-lock.json node_modules/.vite
npm install
npm run codegen
npm run dev -- --force

# Проверить что mock-сервер НЕ запущен
lsof -ti:3000
# Если что-то есть - закройте процесс
```

---

## Дополнительные задания (по желанию)

1. **Валидация essay ответов**:
   - Показывать ошибку если текст короче `minLength`
   - Блокировать ввод при достижении `maxLength`

2. **Превью результата**:
   - После завершения показывать какие вопросы были essay, а какие multiple-select
   - Для essay показывать статус "На проверке у преподавателя"

3. **Улучшение UX для essay**:
   - Автосохранение текста в localStorage
   - Счетчик слов (в дополнение к символам)
   - Markdown поддержка для форматирования

4. **Индикатор прогресса**:
   - Показывать сколько осталось вопросов каждого типа

---

## Полезные ссылки

- [OpenAPI схема проекта](../lr5/quiz-api-schema.yaml)
- [React Query - Mutations](https://tanstack.com/query/latest/docs/react/guides/mutations)
- [MobX - Computed Values](https://mobx.js.org/computeds.html)
- [Vite - Env Variables](https://vitejs.dev/guide/env-and-mode.html)
